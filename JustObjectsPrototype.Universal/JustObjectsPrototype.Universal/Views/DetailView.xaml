<UserControl
    x:Class="JustObjectsPrototype.Universal.Views.DetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:JustObjectsPrototype.Universal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

	<StackPanel>

		<CommandBar x:Name="commandBar" Background="Transparent"
					local:CommandBarBridge.DetailCommandsProperty="{Binding}">

		</CommandBar>
		
		<TextBlock
			Margin="0,8"
			Style="{ThemeResource TitleTextBlockStyle}"
			HorizontalAlignment="Left"
			Text="{Binding Title}"/>

		<TextBlock
			Margin="0,9"
			HorizontalAlignment="Left"
			MaxWidth="560"
			Style="{ThemeResource BodyTextBlockStyle}"
			Text="{Binding Text}" />
		
	</StackPanel>
</UserControl>
